# Databricks Apps installs dependencies inside the hosted app runtime, which
# can reach the public npm registry but not the local Databricks npm proxy.
registry=https://registry.npmjs.org/
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=600000
