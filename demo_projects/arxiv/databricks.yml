bundle:
  name: arxiv-demo

resources:
  apps:
    arxiv-app:
      name: arxiv-app
      description: "Arxiv Knowledge Assistant Curator App"
      # Using app.yaml for command and env vars
      
targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://e2-dogfood.staging.cloud.databricks.com
