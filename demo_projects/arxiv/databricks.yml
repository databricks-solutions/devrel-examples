bundle:
  name: arxiv-demo

resources:
  apps:
    arxiv-app:
      name: arxiv-app
      description: "Arxiv Knowledge Assistant Curator App"
      source_code_path: .

targets:
  dev:
    mode: development
    default: true
    workspace:
      profile: dlcaspers
